Django>=5.0,<5.3
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
celery>=5.4
django-celery-beat>=2.7
redis>=5.0
drf-spectacular>=0.27
gunicorn>=22.0
psycopg2-binary>=2.9
python-dotenv>=1.0
PyJWT>=2.9
requests>=2.32
django-redis>=5.4
# Real OCR for uploaded lab report / prescription images (apps.documents.ocr).
# Requires the `tesseract-ocr` system binary (see backend/Dockerfile; on a
# local dev machine: `brew install tesseract` or `apt install tesseract-ocr`).
pytesseract>=0.3.10
Pillow>=10.0
django-stubs>=5.0
django-stubs-ext>=5.0
# Real semantic (embedding) retrieval for patient-scoped RAG
# (apps.documents.embeddings): TF-IDF + Truncated SVD (LSA), offline and
# deterministic - no LLM/embedding API, per this project's current rules.
scikit-learn>=1.5,<2.0
numpy>=1.26,<3.0

