fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0

# Agent foundation: official Google Gemini SDK (function calling) and an
# HTTP client for calling the existing backend. No LLM inference happens
# without GEMINI_API_KEY being set - see app/agents/README.md.
google-genai>=1.0,<3.0
httpx>=0.27,<1.0
