"""Phase 1 risk-analysis package: a deterministic, rule-based baseline.

See `risk_engine.py` for the scoring rules and `response_builder.py` for how
a `RiskAssessment` is turned into the stable `AIAnalysisResponse` contract.
"""
